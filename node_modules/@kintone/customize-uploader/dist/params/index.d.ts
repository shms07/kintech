import type { Lang } from "../lang";
interface Params {
    username?: string;
    password?: string;
    oAuthToken?: string;
    baseUrl?: string;
    lang: Lang;
}
export declare const inquireParams: ({ username, password, baseUrl, lang, oAuthToken, }: Params) => Promise<any>;
export * from "./init";
