export declare const isUrlString: (str: string) => boolean;
export declare const wait: (ms: number) => Promise<void>;
export declare const parseProxy: (proxy: string) => {
    host: string;
    port: number;
    auth?: {
        username: string;
        password: string;
    };
};
