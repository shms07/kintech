import type { GeneralInputParams } from "./index";
import KintoneApiClient from "../KintoneApiClient";
import type { Lang } from "../lang";
export interface ImportOption {
    lang: Lang;
    proxy: string;
    guestSpaceId: number;
    destDir: string;
}
export interface ImportParams extends GeneralInputParams {
    options: ImportOption;
}
export interface ImportCustomizeManifest {
    app: string;
}
export declare const importCustomizeSetting: (kintoneApiClient: KintoneApiClient, manifest: ImportCustomizeManifest, status: {
    retryCount: number;
}, options: ImportOption) => Promise<void>;
export declare const runImport: (params: ImportParams) => Promise<void>;
